@model SIS.Models.Major

@{
    ViewData["Title"] = "Edit";
}

<div class="card">
    <div class="card-header header-elements-inline">
        <h5 class="card-title font-weight-bold text-green-700">EDIT FORM</h5>
        <div class="header-elements">
            <div class="list-icons">
                <a class="list-icons-item" data-action="reload"></a>
            </div>
        </div>
    </div>

    <form asp-action="Edit">
        <div class="card-body">
            <div class="row font-weight-bold">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="col-md-12">
                    <fieldset>
                        <legend class="font-weight-semibold text-green-700"><i class="icon-box mr-2"></i> MAJOR INPUT</legend>
                    </fieldset>
                    <input asp-for="ID" type="hidden" />
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label asp-for="MajorName" class="control-label"></label> *
                        <div class="input-group">
                            <input asp-for="MajorName" class="form-control" />
                        </div>
                        <span asp-validation-for="MajorName" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label asp-for="MajorCode" class="control-label"></label> *
                        <div class="input-group">
                            <input asp-for="MajorCode" class="form-control" />
                        </div>
                        <span asp-validation-for="MajorCode" class="text-danger"></span>
                    </div>
                </div>

            </div>
        </div>

        <div class="card-footer">
            <div class="row">
                <div class="col-md-6 mb-0">
                    <div class="form-group mb-0">
                        <a asp-action="Index" class="btn btn-link"><i class="icon-backward"></i> Back to List</a>
                    </div>
                </div>
                <div class="col-md-6 text-right mb-0">
                    <div class="form-group mb-0">
                        <button type="submit" value="" class="btn btn-success">Edit <i class="icon-paperplane ml-2"></i></button>
                    </div>
                </div>
            </div>
        </div>
    </form>
</div>

<script type="text/javascript">
    $(document).ready(function () {
        // Single picker
        $('.daterange-single-empty').daterangepicker({
            singleDatePicker: true,
            autoUpdateInput: false,
            locale: {
                cancelLabel: 'Clear',
                format: 'DD-MM-YYYY'
            }
        });

        // Single picker - Empty value
        $('.daterange-single-empty').on('apply.daterangepicker', function (ev, picker) {
            $(this).val(picker.startDate.format('DD-MMM-YYYY'));
        });

        $('.daterange-single-empty').on('cancel.daterangepicker', function (ev, picker) {
            $(this).val('');
        });
    });
</script>

